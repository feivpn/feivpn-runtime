{
  "$comment": [
    "Example payload an AI agent (Cursor / Claude skill) hands to feivpnctl.",
    "Maps onto: feivpnctl ensure-ready --mode global --country JP",
    "",
    "Note: feivpnctl auto-registers the device with the backend on first",
    "run (no token / subscription URL is required from the caller).",
    "Run `feivpnctl countries` to discover which ISO codes are available."
  ],
  "action": "ensure_feivpn_ready",
  "channel": "stable",
  "profile": "global",
  "preferred_country": "JP"
}
